# Monty Project

This project implements a simple Monty bytecode interpreter in C.

## Features
- push operation
- pall operation
- error handling for invalid instructions
- file-based execution

## Compilation

```bash
gcc -Wall -Werror -Wextra -pedantic -Iinclude src/*.c -o monty
